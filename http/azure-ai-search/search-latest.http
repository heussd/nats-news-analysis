POST {{AI_SEARCH_ENDPOINT}}/indexes('{{AI_SEARCH_INDEX_NAME}}')
    /docs
    /search.post.search
    ?api-version={{AI_SEARCH_API_VERSION}}
Content-Type: application/json
api-key: {{AI_SEARCH_API_KEY}}
?? status == 200
{{
    test.status(200);
}}
{
    "filter": "search.ismatch('*github.com', 'baseUrl')",
    "select": "title, excerpt, date, url",
    "orderby": "date desc",
    "top": 200
}